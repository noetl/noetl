{
  "name": "pg_auth_user",
  "type": "postgres",
  "description": "PostgreSQL credential for auth schema user (auth_user role). Used by authentication playbooks to manage user sessions, roles, and permissions in the auth schema.",
  "tags": ["postgres", "auth", "database", "auth0"],
  "data": {
    "db_host": "postgres.default.svc.cluster.local",
    "db_port": "5432",
    "db_user": "auth_user",
    "db_password": "auth_user_temp_password_change_me",
    "db_name": "demo_noetl",
    "db_schema": "auth"
  }
}
