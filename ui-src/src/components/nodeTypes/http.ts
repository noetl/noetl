import { NodeTypeDef } from './NodeType';

export const httpNode: NodeTypeDef = {
    type: 'http',
    label: 'HTTP',
    icon: '🌐',
    color: '#1890ff',
    description: 'Perform an HTTP request to an external service.'
};
