import { NodeTypeDef } from './NodeType';

export const sqlNode: NodeTypeDef = {
    type: 'sql',
    label: 'SQL',
    icon: '🗄️',
    color: '#722ed1',
    description: 'Execute a SQL query against a database.'
};
