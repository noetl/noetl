# NoETL worker pool autoscaler — Phase 4 round 2
#
# Generated by:
#   from noetl.core.runtime.keda import (
#       ScaledObjectSpec, build_worker_scaledobject, dump_scaledobject_yaml,
#   )
#   spec = ScaledObjectSpec(
#       worker_pool_urn="noetl://tenant/default/org/default/worker/worker-cpu-01",
#       deployment="noetl-worker",
#       nats_consumer="noetl_worker_pool",
#   )
#   print(dump_scaledobject_yaml(build_worker_scaledobject(spec)))
#
# The explicit `nats_consumer=` matches the existing
# `noetl-worker-config` ConfigMap (`NATS_CONSUMER=noetl_worker_pool`).
# Fresh worker pools added in the future can omit `nats_consumer` to
# get a URN-derived name automatically — see the wiki page
# `noetl/core/runtime/keda` for the derivation rule.
#
# Sample manifest — committed verbatim so the
# `test_sample_manifest_matches_generator_output` drift guard catches
# any hand-edit. To regenerate after a generator update, re-run the
# snippet above and overwrite this file.
---
apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: noetl-worker-scaler-worker-cpu-01
  namespace: noetl
  labels:
    app: noetl-worker
    worker-pool: worker-cpu-01
    managed-by: noetl
spec:
  scaleTargetRef:
    name: noetl-worker
  minReplicaCount: 1
  maxReplicaCount: 20
  pollingInterval: 10
  cooldownPeriod: 30
  triggers:
  - type: nats-jetstream
    metadata:
      natsServerMonitoringEndpoint: nats.nats.svc.cluster.local:8222
      account: NOETL
      stream: NOETL_COMMANDS
      consumer: noetl_worker_pool
      lagThreshold: '10'
      activationLagThreshold: '1'
      useHttps: 'false'
